package 

func Hashcode(dec string) string {
	size := len(dec)
	hashed := ""

	for _, char := range dec {
		hash := (int(char) + size) % 126
		if hash < 32 || hash > 126 {
			hash +=33
		}
		hashed +=string(hash)
	}
	return hashed
	
}
