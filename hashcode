package 

func Hashcode(dec string) string {
	result := ""

	for _, r := range dec {
		hash := (int(r) + len(dec)) % 127

		if hash < 32 {
			hash += 33
		}
		result += string(rune[hash])
	}
	return result
	
}
